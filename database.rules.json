{
  "rules": {
    ".read": false,
    ".write": false,
    "salas": {
      "$room": {
        ".read": true,
        ".write": true,
        "listeners": {
          "$listener": {
            ".validate": "newData.isBoolean()"
          }
        }
      }
    }
  }
}
